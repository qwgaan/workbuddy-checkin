# ⚠️ 已废弃（DEPRECATED）

**本仓库已停止维护。**

## 为什么废弃

本仓库原方案的工作方式是：读取客户端本地存储的登录令牌 → 解密 → 携带令牌直接调用
签到接口。这条路在 2026-09 之后走不通了：

| 变更 | 影响 |
|------|------|
| 登录令牌改为**非对称加密信封**存储 | 本地无法再解出明文令牌 |
| 接口强制要求 **`X-Device-Token`** 风控头 | 单纯持有令牌也无法通过风控 |
| 接口升级到 `/v2` | 原调用路径失效 |

## 请改用新方案

### 👉 [qwgaan/workbuddy-checkin-cdp](https://github.com/qwgaan/workbuddy-checkin-cdp)

新方案的核心区别是**不碰令牌**：

- 通过 `--remote-debugging-port` 让客户端开放调试端口
- 用 CDP 在渲染进程里调用客户端自己的内部 RPC：`window.__wbInvoke('auth:claimDailyCheckin')`
- 等价于用户手动点击「Buddy加油站 → 签到领积分」，**鉴权全部由客户端自己完成**
- 因此客户端怎么改令牌加密，脚本都不受影响

新仓库还包含：受控测试套件（mock CDP，27 项断言）、Windows 计划任务与原生自动化的
两种调度方案、以及一套分享前的隐私脱敏清单。

---

_本仓库创建于 2026-08-13，废弃于 2026-09-25，仅作历史参考。_
